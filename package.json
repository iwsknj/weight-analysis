{
  "name": "weight-management",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "axios": "^0.26.0",
    "dayjs": "^1.10.8",
    "dotenv": "^16.0.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^14.11.2",
    "concurrently": "^7.0.0",
    "gts": "^3.1.0",
    "nodemon": "^2.0.15",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint",
    "start": "functions-framework --source=build/src/ --target=helloWorld",
    "watch": "concurrently \"tsc -w\" \"nodemon --watch ./build/ --ignore ./build/auth/ --exec npm run start\""
  }
}
